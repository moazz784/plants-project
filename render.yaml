services:
  - type: web
    name: plant-disease-api
    runtime: python
    rootDir: "Ai model"
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GDRIVE_MODEL_FILE_ID
        sync: false
